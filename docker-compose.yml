services:
  sync:
    build: .
    container_name: squarespace-civicrm-sync
    restart: unless-stopped
    env_file: .env
    environment:
      - SYNC_INTERVAL=43200
      - PORT=3000
    ports:
      - "3000:3000"
    volumes:
      - ./cache:/app/cache
      - ./state:/app/state
